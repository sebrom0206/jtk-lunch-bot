[project]
name = "jtk-lunch-bot"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["requests", "python-dotenv"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"